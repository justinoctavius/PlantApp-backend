interface IProductModel {
  product_id: string;
  name: string;
  description: string;
  price: number;
  image_id: string;
  quantity: number;
  category_id: string;
}

export default IProductModel;
